**双指针法**：
关键字：不包含重复

<font color="red">可以利用排序避免重复</font>

降低复杂度变为towSum

利用双指针，一个指向头，一个指向尾

时间复杂度：O(n^2^)

> 排序：O（n $\log$ n)         遍历数组：O（n）         双指针遍历：O（n）
>
> 总体：O（n $\log$ n)+O（n）*O（n）

空间复杂度：O(1)
